import HomeEvents from '.'

export default {
    title: 'Components/Sections/HomeEvents',
    component: HomeEvents,
}

const Template = (args) => <HomeEvents {...args} />
export const Default = Template.bind({})
